# Polish and hand off

Use this bounded finish pass after the source compiles. Preserve product truth, content, ownership,
and the established theme. Fix all findings from one batched desktop/mobile review together, then
confirm once; do not run open-ended visual loops.

Review hierarchy and reading order, section rhythm, text measure, responsive overflow, focus and
keyboard behavior, loading/empty/error states, alt text, media crop and provenance, repeated-data
reuse, and unsupported local props. Keep the tree default-first and use geometry only on the
semantic owner when comparison evidence requires it. Re-run compiler validation after repairs.

For an attached reference, capture again at the reference viewport and inspect the report and diff.
Report compiler status, first and final validation, resolved/pending assets, and visual QA status;
`not_run` is unverified evidence and must never be described as visual parity.
